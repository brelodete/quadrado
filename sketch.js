function setup() {
  // cria uma tela de 4000px de largura por 400px de altura

  createCanvas(400, 400);
}

function draw() {
  background("white");
}
  function draw() {
  stroke("blue") 
  fill("red");
    
  if (mouseIsPressed) {
  rect(mouseX, mouseY, 20, 35);
  }
}
